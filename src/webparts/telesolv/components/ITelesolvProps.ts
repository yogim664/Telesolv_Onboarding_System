import { WebPartContext } from "@microsoft/sp-webpart-base";
export interface ITelesolvProps {
  context: WebPartContext;
  // description: string;
  // isDarkTheme: boolean;
  // environmentMessage: string;
  // hasTeamsContext: boolean;
  // userDisplayName: string;
}
